import React, { Component } from 'react';
import Count from './Count';  // Assuming your Count component is in the same directory
import "./App.css"
export default class App extends Component {
  render() {
    return (
      <div>
        <Count />
      </div>
    );
  }
}

